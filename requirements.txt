# Core dependencies for AlphaZero implementation
torch>=2.0.0
numpy>=1.20.0

# Optional but recommended for better performance
# Uncomment if you want to use GPU acceleration (requires CUDA)
# torch>=2.0.0+cu118 --index-url https://download.pytorch.org/whl/cu118
